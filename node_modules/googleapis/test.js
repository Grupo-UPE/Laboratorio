var google = require('./lib/googleapis.js');
var drive = google.drive('v2');
var fs = require('fs');

var resourceBody = {
    title: 'he',
    mimeType: 'application/json'
};

drive.files.insert({
    resource: resourceBody,
    media: {
        mimeType: 'application/json',
        body: fs.createReadStream('/Users/jbd/Downloads/prog_lang_poster.pdf')
    },
    // auth: oauth2Client
},
function (err, result) {

})